<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.backmeup</groupId>
	<artifactId>plugins</artifactId>
	<version>0.0.1</version>
	<packaging>pom</packaging>

  <prerequisites>
    <maven>3.0.3</maven>
  </prerequisites>

	<modules>
		<module>backmeup-discmailing-plugin</module>
		<module>backmeup-dropbox-plugin</module>
		<module>backmeup-dummy-plugin</module>
		<module>backmeup-facebook-plugin</module>
		<module>backmeup-mail-plugin</module>
		<module>backmeup-moodle-plugin</module>
		<module>backmeup-skydrive-plugin</module>
		<module>backmeup-twitter-plugin</module>
		<module>backmeup-zip-plugin</module>
	</modules>

	<properties>
		<java.compiler.source>1.7</java.compiler.source>
		<java.compiler.target>1.7</java.compiler.target>
    <project.build.java.target>1.7</project.build.java.target>

    <encoding>UTF-8</encoding>
    <project.build.sourceEncoding>${encoding}</project.build.sourceEncoding>
    <project.reporting.outputEncoding>${encoding}</project.reporting.outputEncoding>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>2.2</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
          <source>${project.build.java.target}</source>
          <target>${project.build.java.target}</target>
          <encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.5</version>
				<configuration>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
      <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>2.2</version>
          <!--
          <configuration>
              <failOnWarning>true</failOnWarning>
          </configuration>
          -->
          <executions>
               <execution>
                   <id>check-dependencies</id>
                   <phase>verify</phase>
                   <goals>
                       <goal>analyze</goal>
                   </goals>
               </execution>
           </executions>
       </plugin>
		</plugins>
	</build>
</project>
